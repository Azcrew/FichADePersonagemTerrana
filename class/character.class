<?php
    class Character{
        protected $character = array();

        function _construct($character)
        {
            $this->character = $character;
        }

        function setId()
        {

        }

        function getId()
        {

        }

        function setName($name)
        {
            $this->character['name'] = $name; 
        }

        function setLevel($level)
        {
            $this->character['level'] = $level; 
        }

        function setPoints($points)
        {
            $this->character['points'] = $points; 
        }

        function setRace($race)
        {
            $this->character['race'] = $race;
        }

        function setClass($class)
        {
            $this->character['class'] = $class;
        }

        function setAdventure($adventure)
        {
            $this->character['adventure'] = $adventure;
        }

        function setAspects($armor, $focus, $strength, $ability, $resistence, $accuracy)
        {
            $aspects = array(
                'armor' => $armor,
                'focus' => $focus,
                'strength' => $strength,
                'ability' => $ability,
                'resistence' => $resistence,
                'accuracy' => $accuracy
            );
            $this->character['aspects'] = json_encode($aspects);
        }

        function setFocus($water, $air, $fire, $ligth, $earth, $darkness)
        {
            $focus = array(
                'water' => $water,
                'air' => $air,
                'fire' => $fire,
                'ligth' => $ligth,
                'earth' => $earth,
                'darkness' => $darkness
            );
            $this->character['focus'] = json_encode($focus);
        }

        function setBenefit($benefit)
        {
            $this->character['benefit'] = json_encode($benefit, JSON_FORCE_OBJECT);
        }
        function addBenefit(){ }
        function removeBenefit(){ }

        function setInjury($injury)
        {   
            $this->character['injury'] = json_encode($injury, JSON_FORCE_OBJECT);
        }
        function addInjury(){ }
        function removeInjury(){ }

        function setKnowledge($knowledge)
        {
            $this->character['knowledge'] = json_encode($knowledge, JSON_FORCE_OBJECT);
        }
        function addKnowledge(){ }
        function levelUpKnowledge(){ }


        function addItem($itemId){ }
        function removeItem($itemId){ }
        function levelUpItem($itemId){ }

        function setHistory($history)
        {
            $this->character['history'] = $history;
        }
        function addHistory()
        {

        }

        function getEncryptionCode($data)
        {
            return $code;
        }

        function checkEncryptionCode($data)
        {

        }

        function getCharacter()
        {  
            $this->sha512($this->character);
            return $this->character;
        }
    }
?>